export interface Emotion {
    name: string;        // Nombre de la emoción
    count: number;       // Contador de cuantas veces se seleccionó
    lastUpdated: Date;   // Última vez que fue actualizada
  }